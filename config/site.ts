import { SiteConfig } from "@/types"

export const siteConfig: SiteConfig = {
  name: "Master Frontend Foundation",
  description: "Universal, domain-independent premium frontend package system & AI design skills framework.",
  url: "https://localhost:3000",
  ogImage: "/og-image.png",
  links: {
    github: "https://github.com",
    docs: "/docs",
  },
}
